let number = prompt('Enter a number:');
var multiple;
let answer;
for (multiple = 1; multiple <=10; multiple ++){
    answer = number * multiple
    console.log(number +"*" + multiple + "=" + answer )
}
